spring.application.name=JNews

# ===============================
# Database Configuration (PostgreSQL)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/jpantry
spring.datasource.username=YOUR_DB_USERNAME
spring.datasource.password=YOUR_DB_PASSWORD

# ===============================
# JPA / Hibernate Settings
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Server Settings
# ===============================
server.port=5000
server.error.include-stacktrace=ALWAYS
logging.level.root=DEBUG

# ===============================
# Mail Server Settings
# ===============================
spring.mail.host=smtp.gmail.com   # or change to any host of your choosing. You'll need to reconfigure the other defaults.
spring.mail.port=587
spring.mail.username=CHANGE_TO_REAL_GMAIL_ACCOUNT
spring.mail.password=CHANGE_TO_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.timeout=50000
spring.mail.from=CHANGE_TO_REAL_GMAIL_ACCOUNT

# ===============================
# JWT / Security (example placeholders)
# ===============================
jwt.secret=CHANGE_ME_TO_256_SECRET
jwt.expiration=86400000   # 24 hours in milliseconds